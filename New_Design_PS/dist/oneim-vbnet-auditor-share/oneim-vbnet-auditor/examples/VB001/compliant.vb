Option Explicit On
' VB001 compliant: Option Explicit On is declared.
' All variables must be declared before use.

Imports System

Public Module VB001_Compliant
    Public Sub Run()
        Dim userName As String = "Alice"
        Console.WriteLine(userName)
    End Sub
End Module
